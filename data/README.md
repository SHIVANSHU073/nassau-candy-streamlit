# Application Data

The Streamlit application intentionally does not contain the raw order-level CSV.

Historical aggregates and model artifacts are supplied by the versioned `nassau-candy-analysis` package.
